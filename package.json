{
  "name": "marcellomaranzan-com",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public",
    "db:init": "wrangler d1 execute marcellomaranzan-db --file=schema.sql",
    "db:init:local": "wrangler d1 execute marcellomaranzan-db --file=schema.sql --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
